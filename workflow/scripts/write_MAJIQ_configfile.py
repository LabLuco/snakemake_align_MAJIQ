# Write the config files to run the Snakemake pipeline :
#     - config.yaml to get variables in the Snakemake pipeline (such as path of input files)

import argparse
import os
import glob
import re
import yaml

def write_configsnakemake(scriptdir,rep):
    return

if __name__ == "__main__":
    print('Hi')